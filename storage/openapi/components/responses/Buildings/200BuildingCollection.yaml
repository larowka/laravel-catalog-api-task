description: Ok
content:
  application/json:
    schema:
      type: object
      required:
        - meta
        - data
      properties:
        meta:
          $ref: '../../schemas/Meta.yaml'
        data:
          $ref: '../../schemas/Collections/BuildingCollection.yaml'
