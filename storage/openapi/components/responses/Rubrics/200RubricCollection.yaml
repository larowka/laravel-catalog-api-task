description: Ok
content:
  application/json:
    schema:
      type: object
      properties:
        meta:
          $ref: '../../schemas/Meta.yaml'
        data:
          $ref: '../../schemas/Collections/RubricCollection.yaml'
